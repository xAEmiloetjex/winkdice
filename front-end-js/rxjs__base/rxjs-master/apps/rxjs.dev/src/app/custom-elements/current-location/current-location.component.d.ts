import { LocationService } from 'app/shared/location.service';
/** Renders the current location path. */
export declare class CurrentLocationComponent {
    location: LocationService;
    constructor(location: LocationService);
}
