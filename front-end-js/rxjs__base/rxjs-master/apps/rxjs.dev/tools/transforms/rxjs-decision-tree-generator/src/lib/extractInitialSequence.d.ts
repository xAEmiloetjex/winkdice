import { TreeNode } from './interfaces';
/**
 * Strip out initial sequence and add to tree
 *
 * @export
 * @param {Tree} tree
 * @returns {{id: string, options: string[]}}
 */
export declare function extractInitialSequence(tree: TreeNode[]): {
    id: string;
    options: string[];
};
